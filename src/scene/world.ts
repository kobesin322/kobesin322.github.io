import type { Vec3 } from "../types";

/** Dark globe under the constellation — Starlink / Cosmos-style depth cue. */
export const PLANET_CENTER: Vec3 = [0, -11.6, -2.8];
export const PLANET_RADIUS = 6.4;
