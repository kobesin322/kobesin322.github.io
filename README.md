# site

Personal site for Kobe Sin.

Static HTML / CSS / JS. No build step. English only.

```
index.html
styles.css
app.js
```

Open `index.html` or:

```bash
python3 -m http.server 5173
```

Work descriptions are public-layer only. Employer names are omitted on purpose.
